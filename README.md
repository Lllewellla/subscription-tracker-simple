# 📱 Трекер подписок

Веб-приложение для отслеживания подписок и управления ими. Работает прямо в браузере без установки зависимостей.

**Версия:** 1.3  
**Последнее обновление:** Январь 2026

---

## 🎯 Для новой сессии Cursor - Важная информация

### ⚡ Быстрые ссылки
- **Главный файл приложения:** `app.js` (вся логика приложения)
- **Синхронизация с облаком:** `firebase-sync.js` (Firebase интеграция)
- **Настройка Firebase:** `FIREBASE_SETUP.md` (подробная инструкция)
- **Стили:** `styles.css` (природный, адаптивный), `pixel-art.css` (pixel-art)
- **HTML:** `index.html` (структура страницы)

### 🔑 Ключевые особенности текущей версии

1. **Облачная синхронизация через Firebase:**
   - Аутентификация по email/password (один аккаунт на всех устройствах)
   - Явное управление: кнопки "💾 Сохранить в облако" и "⬇️ Обновить из облака"
   - Автоматическая синхронизация отключена (только явное управление)
   - Защита от конфликтов версий с проверкой временных меток

2. **Исправления синхронизации:**
   - ✅ Исправлен критический баг: при загрузке из облака сохраняется временная метка из облака
   - ✅ Локальные неактуальные данные больше не перезаписывают актуальные из облака
   - ✅ Добавлено логирование для отладки синхронизации

3. **Мобильная адаптивность:**
   - ✅ Полностью адаптивный дизайн для мобильных устройств
   - ✅ Исправлено наслаивание плашек управления
   - ✅ Оптимизация для touch-устройств

4. **Импорт подписок:**
   - ✅ Поддержка простого формата (каждая строка - отдельная транзакция)
   - ✅ Создание кандидатов даже из одной транзакции
   - ✅ Определение годовых подписок по ключевым словам ("year")

5. **Автоматическое управление просроченными подписками:**
   - ✅ Автоматический перенос даты следующего списания при просрочке
   - ✅ Подписки с просроченной датой автоматически переносятся на следующий период (месяц/год)
   - ✅ Карточки с перенесённой датой автоматически уходят в конец списка
   - ✅ Подсветка "просрочено" снимается после переноса даты

6. **Статистика:**
   - ✅ Общая сумма в рублях с автоматической конвертацией валют
   - ✅ Курсы: EUR → RUB (×100), USD → EUR (×0.90) → RUB (×100)
   - ✅ Порядок карточек: Всего подписок → Общая сумма в рублях → Всего в месяц → остальные

7. **Исправления для мобильных устройств:**
   - ✅ Исправлена ошибка "Can't find variable: Notification" на мобильных браузерах
   - ✅ Добавлены проверки доступности Notification API перед использованием

### 📋 Структура данных

**Подписка:**
```javascript
{
    id: string,
    name: string,
    price: number,
    currency: string,  // ₽, $, €
    nextBillingDate: string,  // ISO: YYYY-MM-DD (автоматически переносится при просрочке)
    billingCycle: 'monthly' | 'yearly',
    group: 'mine' | 'others',
    excludeFromStats: boolean,
    notes?: string
}
```

**Важно:** Поле `isPaid` больше не используется. Подписки с просроченной датой автоматически считаются оплаченными, дата переносится на следующий период.

**localStorage ключи:**
- `subscriptions` - массив подписок
- `subscription-tracker-theme` - выбранный стиль (nature/pixel)
- `subscriptions_timestamp` - временная метка для синхронизации

### 🔧 Важные функции

**app.js:**
- `saveSubscriptions(subscriptions, timestamp)` - сохранить с опциональной временной меткой
- `loadSubscriptions()` - загрузить из localStorage (НЕ из облака автоматически)
- `renderSubscriptions()` - рендеринг подписок с автоматическим переносом просроченных дат
- `convertToRubles(amount, currency)` - конвертация суммы в рубли для статистики
- `addMonths(iso, months)` - добавление месяцев к дате (ISO формат)
- `addYears(iso, years)` - добавление лет к дате (ISO формат)

**firebase-sync.js:**
- `saveToCloudExplicit()` - явное сохранение в облако
- `loadFromCloudExplicit()` - явная загрузка из облака
- `getCloudTimestamp()` - получить временную метку из облака
- `setupSyncListener()` - слушатель изменений в облаке (real-time)

### ⚠️ Важные моменты

1. **Временные метки:**
   - При загрузке из облака ВСЕГДА передавать временную метку из облака в `saveSubscriptions(data, cloudTimestamp)`
   - НЕ использовать `Date.now()` при загрузке из облака
   - Это критично для правильной работы синхронизации

2. **Автоматическая синхронизация:**
   - Отключена в `saveSubscriptions()` - данные сохраняются только локально
   - Синхронизация происходит только через кнопки или при входе в систему

3. **Автоматический перенос даты при просрочке:**
   - При каждом рендере проверяются все подписки
   - Если `nextBillingDate < сегодня`, дата автоматически переносится на следующий период
   - Для `monthly` подписок: добавляется 1 месяц
   - Для `yearly` подписок: добавляется 1 год
   - Если подписка просрочена на несколько периодов, дата переносится в цикле до первой будущей даты
   - После переноса карточка уходит в конец списка (сортировка по дате)
   - Подсветка "просрочено" автоматически снимается

4. **Мобильная версия:**
   - `.app` имеет `padding-top: 80px` (100px на мобильных) для плашек управления
   - Плашки управления в `.header-controls` с `position: fixed`
   - Адаптивные стили в `@media (max-width: 768px)` и `@media (max-width: 480px)`
   - Notification API проверяется перед использованием (исправлена ошибка на iOS Safari)

### 🐛 Известные проблемы и решения

**Проблема:** Локальные данные перезаписывают облачные
- **Решение:** Исправлено - теперь сохраняется временная метка из облака

**Проблема:** Плашки управления наслаиваются на контент
- **Решение:** Исправлено - добавлен padding-top для .app

**Проблема:** Импорт не работает с простым форматом
- **Решение:** Исправлено - добавлена поддержка простого формата (каждая строка - транзакция)

---

## 🚀 Быстрый старт

1. **Откройте файл `index.html` в браузере**
   - Дважды кликните на файл `index.html`
   - Или откройте через браузер (Chrome, Edge, Firefox)

2. **Готово!** Приложение сразу готово к использованию.

## 📁 Структура проекта

```
subscription-tracker-simple/
├── index.html          # Главная HTML страница (включает Firebase SDK)
├── styles.css          # Природный стиль (основной, адаптивный)
├── pixel-art.css       # Pixel-art стиль (альтернативный)
├── app.js              # Вся логика приложения (JavaScript)
├── firebase-sync.js    # Модуль синхронизации с Firebase (опционально)
├── FIREBASE_SETUP.md   # Инструкция по настройке Firebase
├── bot/                # Telegram-бот (уведомления, /list, /add, /delete)
│   ├── index.js        # Логика бота
│   ├── README.md       # Настройка и запуск бота
│   └── .env.example    # Пример переменных окружения
├── README.md           # Этот файл
├── .gitignore          # Игнорируемые файлы Git
├── .vscode/            # Настройки VS Code
│   └── tasks.json      # Задачи для VS Code
└── [другие файлы]      # Вспомогательные скрипты и файлы
```

## 🎨 Визуальные стили

Приложение поддерживает два визуальных стиля с возможностью переключения:

### 🌿 Природный стиль (основной)
- Спокойная природная палитра (зеленые оттенки)
- Плавные переходы и мягкие тени
- Скругленные углы
- Полупрозрачные элементы с эффектом размытия
- Современный минималистичный дизайн

### 🎮 Pixel Art стиль (альтернативный)
- Ретро-игровая эстетика
- Пиксельные шрифты (Press Start 2P, VT323)
- Четкие границы без скруглений
- Ступенчатые тени
- Яркая контрастная палитра

**Переключение стилей**: Кнопки в правом верхнем углу (🌿 Природа / 🎮 Pixel Art)
- Выбор сохраняется в localStorage
- Применяется автоматически при следующем открытии

## 🎯 Основные возможности

- ✅ Добавление и редактирование подписок
- ✅ Отслеживание стоимости каждой подписки
- ✅ Отображение даты следующего списания
- ✅ Уведомления за 2 дня до списания
- ✅ Расчет месячных трат (с поддержкой разных валют)
- ✅ Группировка подписок (мои / для других людей)
- ✅ **Галочка "не учитывать в статистике"** - исключение подписок из расчетов
- ✅ Импорт из скриншотов выписки (OCR через Tesseract.js)
- ✅ Импорт из текстового списка списаний (поддержка простого формата)
- ✅ Два режима отображения: сетка и список
- ✅ Автоматическое выделение подписок с предстоящим списанием
- ✅ Переключатель визуальных стилей (природный / pixel-art)
- ✅ **Облачная синхронизация** через Firebase (опционально)
- ✅ **Аутентификация по email/password** для доступа с любого устройства
- ✅ **Явное управление синхронизацией** (кнопки сохранения/загрузки)
- ✅ **Защита от конфликтов** версий данных
- ✅ **Мобильная адаптивная версия**
- ✅ **Telegram-бот** — уведомления за 2 дня до списания, список подписок, добавление и удаление через бота. Деплой в облако (Railway) — см. `bot/README.md`

## 💻 Разработка

### Технологии

- **HTML5** - структура страницы
- **CSS3** - стилизация (flexbox, grid, градиенты, backdrop-filter)
- **Vanilla JavaScript** - вся логика без фреймворков
- **Tesseract.js** (CDN) - OCR для распознавания текста из изображений
- **localStorage** - локальное хранение данных
- **Firebase Firestore** (опционально) - облачная синхронизация данных
- **Firebase Authentication** (опционально) - аутентификация пользователей
- **Google Fonts** - шрифты (Press Start 2P, VT323 для pixel-art стиля)

### Основные функции в app.js

#### Хранение данных
- `saveSubscriptions(subscriptions)` - сохранить подписки в localStorage
- `loadSubscriptions()` - загрузить подписки из localStorage

#### Рендеринг
- `render()` - перерисовать весь интерфейс
- `renderStatistics()` - отобразить статистику (исключает подписки с `excludeFromStats: true`)
- `renderSubscriptions()` - отобразить список подписок

#### Управление подписками
- `openForm(sub)` - открыть форму добавления/редактирования
- `closeForm()` - закрыть форму
- `handleFormSubmit(e)` - обработать сохранение подписки
- `editSubscription(id)` - редактировать подписку
- `deleteSubscription(id)` - удалить подписку

#### Фильтрация и отображение
- `setFilter(filter)` - установить фильтр (all/mine/others)
- `setViewMode(mode)` - установить режим отображения (grid/list)

#### Переключение стилей
- `setTheme(theme)` - переключить визуальный стиль (nature/pixel)
- `initTheme()` - инициализировать тему при загрузке

#### Импорт
- `parseStatementText(text)` - парсить текст выписки (поддерживает простой формат и формат "Плати по миру")
- `parseAmount(s)` - извлечь сумму из строки
- `parseDateToIso(input, fallbackYear)` - преобразовать дату в ISO формат
- `buildSubscriptionCandidates(transactions)` - найти кандидатов на подписки (создает даже из одной транзакции)
- `inferCycle(datesIso)` - определить цикл подписки (monthly/yearly) по датам
- `parseFromText(text)` - обработать текст и найти подписки
- `applyImport()` - применить импорт подписок
- `autoImportSubscriptions()` - автоматический импорт (доступна в консоли)

#### Синхронизация (firebase-sync.js)
- `initSync()` - инициализация синхронизации
- `loginWithEmail(email, password)` - вход по email/password
- `registerWithEmail(email, password)` - регистрация
- `saveToCloudExplicit()` - явное сохранение в облако
- `loadFromCloudExplicit()` - явная загрузка из облака
- `syncToCloud()` - внутренняя функция сохранения
- `loadFromCloud()` - внутренняя функция загрузки
- `getCloudTimestamp()` - получить временную метку из облака
- `setupSyncListener()` - слушатель изменений в облаке

#### Уведомления
- `requestNotificationPermission()` - запросить разрешение на уведомления
- `checkUpcomingBilling(subscriptions)` - проверить предстоящие списания

### Как добавить новую функцию

1. **Откройте `app.js`** в редакторе
2. Найдите нужную секцию (например, функции рендеринга)
3. Добавьте новую функцию
4. Вызовите её в нужном месте (например, в `init()` или обработчике события)
5. Обновите страницу в браузере

### Как изменить стили

#### Природный стиль (основной)
1. **Откройте `styles.css`**
2. Найдите нужный класс (используйте поиск по названию элемента)
3. Измените стили
4. Обновите страницу (Ctrl+F5 для жесткой перезагрузки)

#### Pixel-art стиль
1. **Откройте `pixel-art.css`**
2. Измените стили аналогично
3. Переключитесь на pixel-art стиль в интерфейсе для проверки

### Как добавить новое поле в подписку

1. **В `index.html`** - добавьте поле в форму (внутри `#subscription-form`)
2. **В `app.js`**:
   - Обновите `handleFormSubmit()` - добавьте чтение нового поля
   - Обновите `renderSubscriptions()` - добавьте отображение нового поля
   - Обновите `openForm()` - добавьте заполнение поля при редактировании
   - Обновите `applyImport()` - добавьте обработку при импорте

### Отладка

1. **Откройте консоль браузера** (F12)
2. Проверьте ошибки в консоли
3. Используйте `console.log()` для отладки
4. Проверьте localStorage: `localStorage.getItem('subscriptions')`
5. Проверьте сохраненную тему: `localStorage.getItem('subscription-tracker-theme')`

## 📊 Структура данных подписки

```javascript
{
    id: string,                    // Уникальный ID
    name: string,                  // Название подписки
    price: number,                 // Стоимость
    currency: string,              // Валюта (₽, $, €)
    nextBillingDate: string,       // Дата следующего списания (ISO: YYYY-MM-DD)
    billingCycle: 'monthly' | 'yearly',  // Период оплаты
    group: 'mine' | 'others',      // Группа подписки
    excludeFromStats: boolean,      // Не учитывать в статистике (новое поле)
    notes?: string                 // Заметки (опционально)
}
```

## 🔧 Полезные команды в консоли браузера

```javascript
// Импортировать подписки из СМС
autoImportSubscriptions()

// Посмотреть все подписки
console.log(subscriptions)

// Переключить стиль программно
setTheme('nature')  // или 'pixel'

// Очистить все данные
localStorage.removeItem('subscriptions')
localStorage.removeItem('subscription-tracker-theme')
localStorage.removeItem('subscriptions_timestamp')
location.reload()

// Экспортировать данные
JSON.stringify(JSON.parse(localStorage.getItem('subscriptions')), null, 2)

// Импортировать данные
localStorage.setItem('subscriptions', JSON.stringify([...]))
localStorage.setItem('subscriptions_timestamp', Date.now().toString())
location.reload()

// Синхронизация (если Firebase настроен)
window.firebaseSync.saveToCloudExplicit()  // Сохранить в облако
window.firebaseSync.loadFromCloudExplicit()  // Загрузить из облака
window.firebaseSync.isEnabled()  // Проверить, включена ли синхронизация
```

## 🎨 Режимы отображения

- **Сетка (grid)** - карточки в сетке, удобно для просмотра
- **Список (list)** - карточки в одну строку, компактно

Переключение через кнопки в интерфейсе или программно:
```javascript
setViewMode('grid')  // или 'list'
```

## 📝 Формат импорта текста

Приложение умеет парсить различные форматы выписок. Примеры:

```
12.01.2026 Netflix 399,00 ₽
13.01.2026 Spotify 199 ₽

Плати по миру, [13.12.2025 14:24]
Покупка: 34.37 €. Карта: *2912. KREA.AI INC.
```

Главное - наличие даты и суммы в строке.

## 🔔 Уведомления

Приложение запрашивает разрешение на уведомления при первом запуске.
Уведомления показываются за 2 дня до списания.

## 💾 Хранение данных

### Локальное хранение
По умолчанию все данные хранятся в `localStorage` браузера:
- **`subscriptions`** - массив подписок
- **`subscription-tracker-theme`** - выбранный стиль (nature/pixel)
- **`subscriptions_timestamp`** - временная метка последнего обновления (для синхронизации)

### ☁️ Облачная синхронизация (опционально)

Приложение поддерживает синхронизацию данных через Firebase Firestore, что позволяет:
- ✅ Доступ к данным с любого устройства (один аккаунт на всех устройствах)
- ✅ Резервное копирование в облаке
- ✅ Работа в офлайн режиме с последующей синхронизацией
- ✅ Явное управление синхронизацией (кнопки сохранения/загрузки)

**Настройка синхронизации:**
1. Следуйте инструкциям в файле `FIREBASE_SETUP.md`
2. Настройте Firebase проект и добавьте конфигурацию в `index.html`
3. Включите Email/Password аутентификацию в Firebase Console
4. Добавьте ваш домен в "Authorized domains" (если используете GitHub Pages)
5. Нажмите кнопку **"Войти для синхронизации"** в правом верхнем углу

**Как работает синхронизация:**
- **При входе**: Автоматически загружаются данные из облака (если они новее локальных)
- **Кнопка "💾 Сохранить в облако"**: Явно сохраняет локальные данные в облако
- **Кнопка "⬇️ Обновить из облака"**: Явно загружает данные из облака
- **Защита от конфликтов**: Перед сохранением/загрузкой проверяются временные метки и показывается предупреждение

**Важно**: 
- Без настройки Firebase приложение работает в локальном режиме
- Автоматическая синхронизация отключена - используйте кнопки для явного управления
- При очистке данных браузера все подписки будут удалены (если не используется синхронизация)
- С синхронизацией данные сохраняются в облаке и доступны на всех устройствах с одним email/password

## ✨ Особенности реализации

### Галочка "не учитывать в статистике"
- Добавлена в форму добавления/редактирования подписки
- Сохраняется в поле `excludeFromStats` объекта подписки
- При расчете статистики подписки с `excludeFromStats: true` исключаются из:
  - Общего количества подписок
  - Общих месячных трат
  - Трат по группам (мои/для других)
- В карточке подписки отображается бейдж "Не в статистике" если галочка установлена

### Переключение стилей
- Два CSS файла: `styles.css` (природный) и `pixel-art.css` (pixel-art)
- Переключение через `disabled` атрибут на `<link>` элементах
- Выбор сохраняется в localStorage под ключом `subscription-tracker-theme`
- При переключении обновляются заголовки страницы (текст и стиль)
- Переключатель стилизован в соответствии с текущим стилем

### Статистика
- Рассчитывается только для подписок без галочки `excludeFromStats`
- Поддерживает несколько валют одновременно
- Автоматически конвертирует годовые подписки в месячные для сравнения
- Показывает отдельно траты по группам (мои/для других)

## 🐛 Решение проблем

### Страница пустая
- Откройте консоль (F12) и проверьте ошибки
- Убедитесь, что файлы `styles.css`, `pixel-art.css` и `app.js` в той же папке
- Проверьте, что открываете файл из папки `subscription-tracker-simple`

### Импорт не работает
- Проверьте подключение к интернету (для Tesseract.js)
- Убедитесь, что в тексте есть даты и суммы
- Проверьте консоль на ошибки
- Формат должен быть: `DD.MM.YYYY Название Сумма Валюта`

### Уведомления не работают
- Проверьте разрешения браузера для уведомлений
- Убедитесь, что браузер поддерживает уведомления

### Стили не переключаются
- Проверьте консоль на ошибки загрузки CSS файлов
- Убедитесь, что оба файла `styles.css` и `pixel-art.css` существуют
- Проверьте localStorage: `localStorage.getItem('subscription-tracker-theme')`

### Проблемы с синхронизацией
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что Firebase настроен правильно (см. `FIREBASE_SETUP.md`)
- Проверьте, что Email/Password аутентификация включена в Firebase Console
- Убедитесь, что ваш домен добавлен в "Authorized domains"
- Проверьте правила Firestore - они должны быть опубликованы
- Используйте кнопки "💾 Сохранить в облако" и "⬇️ Обновить из облака" для явного управления

### Локальные данные перезаписывают облачные
- **ИСПРАВЛЕНО**: Теперь при загрузке из облака сохраняется временная метка из облака
- Проверьте консоль на логи сравнения версий
- Используйте кнопку "⬇️ Обновить из облака" для загрузки актуальных данных

## 📚 Дополнительная информация

### Зависимости (загружаются через CDN)
- **Tesseract.js** - для OCR (распознавание текста из изображений)
  - Загружается автоматически при открытии страницы
  - Требует интернет для первой загрузки
- **Google Fonts** - шрифты для pixel-art стиля
  - Press Start 2P (для заголовков)
  - VT323 (для основного текста)

### Совместимость браузеров
- Chrome/Edge (рекомендуется)
- Firefox
- Safari
- Opera

### Производительность
- Приложение работает полностью локально
- Нет запросов к серверу (кроме загрузки Tesseract.js и шрифтов)
- Быстрая работа даже с большим количеством подписок
- Переключение стилей происходит мгновенно

## 🎨 Детали стилей

### Природный стиль (styles.css)
- Цветовая палитра: зеленые оттенки (#e8f5e9 - #66bb6a)
- Градиентный фон с фиксированным позиционированием
- Полупрозрачные элементы (rgba с backdrop-filter)
- Скругления: 12-20px
- Мягкие тени с зеленым оттенком
- Плавные переходы (0.3s ease)

### Pixel-art стиль (pixel-art.css)
- Цветовая палитра: темный фон (#1a1a2e) с яркими акцентами
- Пиксельные эффекты (image-rendering: pixelated)
- Четкие границы (4px solid)
- Ступенчатые тени (box-shadow без blur)
- Без скруглений (или минимальные)
- Отключено сглаживание шрифтов
- Анимации в pixel-art стиле

## 🚀 Планы развития

Возможные улучшения:
- ✅ Резервное копирование в облако (реализовано через Firebase)
- ✅ Мобильная адаптивная версия (реализовано)
- ✅ Явное управление синхронизацией (реализовано)
- Экспорт данных в JSON/CSV
- Дополнительные темы оформления
- Категории подписок
- Графики трат
- История платежей
- Напоминания по email/SMS
- Темная тема для природного стиля
- Улучшенная обработка конфликтов версий (слияние изменений)

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что используете последнюю версию файлов
3. Проверьте формат данных в localStorage
4. Проверьте наличие всех файлов (index.html, styles.css, pixel-art.css, app.js)

## 🔄 Синхронизация данных (Firebase)

### Архитектура синхронизации

**Модуль:** `firebase-sync.js`

**Основные функции:**
- `initSync()` - инициализация синхронизации при загрузке
- `loginWithEmail(email, password)` - вход по email/password
- `registerWithEmail(email, password)` - регистрация нового пользователя
- `saveToCloudExplicit()` - явное сохранение в облако (кнопка "💾 Сохранить в облако")
- `loadFromCloudExplicit()` - явная загрузка из облака (кнопка "⬇️ Обновить из облака")
- `syncToCloud()` - внутренняя функция сохранения в Firestore
- `loadFromCloud()` - внутренняя функция загрузки из Firestore
- `getCloudTimestamp()` - получение временной метки из облака
- `setupSyncListener()` - слушатель изменений в облаке (real-time)

**Ключевые исправления:**
- ✅ Исправлен баг с временными метками: при загрузке из облака сохраняется метка из облака, а не текущее время
- ✅ Предотвращена перезапись актуальных данных из облака локальными неактуальными
- ✅ Добавлена проверка конфликтов версий перед сохранением/загрузкой
- ✅ Автоматическая синхронизация отключена - только явное управление через кнопки

**Структура данных в Firestore:**
```
users/{userId}/
  - subscriptions: Array<Subscription>
  - lastUpdated: Timestamp
```

**Временные метки:**
- Локальная метка хранится в `localStorage.subscriptions_timestamp`
- Облачная метка хранится в Firestore как `lastUpdated` (serverTimestamp)
- При сравнении используется числовое значение (миллисекунды)

## 📝 История изменений

### Версия 1.3 (Текущая)
- ✅ Исправлена ошибка "Can't find variable: Notification" на мобильных устройствах
- ✅ Добавлена общая сумма в рублях в статистике с автоматической конвертацией валют
- ✅ Реализован автоматический перенос даты следующего списания при просрочке
- ✅ Удалён чекбокс "Оплачено" - просроченные подписки автоматически считаются оплаченными
- ✅ Улучшена сортировка подписок: только по дате следующего списания (хронологически)
- ✅ Карточки с перенесённой датой автоматически уходят в конец списка
- ✅ Исправлена нормализация дат для корректной сортировки
- ✅ Обновлён порядок карточек в статистике: Общая сумма в рублях на втором месте

### Версия 1.2
- ✅ Исправлен критический баг синхронизации (временные метки)
- ✅ Добавлена кнопка "⬇️ Обновить из облака"
- ✅ Улучшена защита от конфликтов версий
- ✅ Добавлено логирование для отладки синхронизации
- ✅ Улучшена мобильная адаптивность
- ✅ Исправлено наслаивание плашек управления

### Версия 1.1
- ✅ Добавлен природный стиль как основной
- ✅ Добавлен переключатель стилей (природный / pixel-art)
- ✅ Добавлена галочка "не учитывать в статистике"
- ✅ Улучшена статистика (исключение подписок из расчетов)
- ✅ Добавлена облачная синхронизация через Firebase
- ✅ Добавлена аутентификация по email/password
- ✅ Добавлена кнопка "💾 Сохранить в облако"
- ✅ Улучшен импорт из текста (поддержка простого формата)

### Версия 1.0
- ✅ Базовый функционал трекера подписок
- ✅ Pixel-art стиль
- ✅ Импорт из выписок
- ✅ Уведомления о предстоящих списаниях

---

**Версия**: 1.3  
**Дата обновления**: Январь 2026  
**Лицензия**: Свободное использование

---

## 🆕 Последние изменения (версия 1.3)

### Автоматическое управление просроченными подписками
Приложение автоматически обрабатывает просроченные подписки:
- При открытии приложения все подписки с датой списания в прошлом автоматически переносятся на следующий период
- Дата пересчитывается согласно типу подписки (ежемесячно/ежегодно)
- Карточки с перенесённой датой автоматически перемещаются в конец списка
- Подсветка "просрочено" снимается, карточка становится обычной

### Статистика в рублях
- Добавлена карточка "Общая сумма в рублях" на втором месте в статистике
- Автоматическая конвертация всех валют в рубли:
  - EUR → RUB: ×100
  - USD → EUR: ×0.90 → RUB: ×100
  - RUB → RUB: ×1

### Исправления для мобильных
- Исправлена критическая ошибка на iOS Safari и других мобильных браузерах
- Добавлены проверки доступности Notification API перед использованием
- Все вызовы Notification обернуты в try-catch для безопасной обработки ошибок
