# 🔥 Настройка Firebase для синхронизации данных

Это руководство поможет вам настроить Firebase для синхронизации данных между устройствами.

## Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите **"Добавить проект"** или **"Create a project"**
3. Введите название проекта (например, `subscription-tracker`)
4. Отключите Google Analytics (не обязательно, но можно отключить для простоты)
5. Нажмите **"Создать проект"**

## Шаг 2: Настройка Firestore Database

1. В меню слева выберите **"Firestore Database"**
2. Нажмите **"Создать базу данных"** или **"Create database"**
3. Выберите режим **"Начать в тестовом режиме"** (Start in test mode)
4. Выберите регион (например, `europe-west` или ближайший к вам)
5. Нажмите **"Включить"** или **"Enable"**

### ⚠️ Важно: Настройка правил безопасности

После создания базы данных перейдите на вкладку **"Правила"** (Rules) и замените правила на следующие:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешаем чтение и запись только аутентифицированным пользователям
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

Нажмите **"Опубликовать"** (Publish) для сохранения правил.

## Шаг 3: Настройка Authentication

1. В меню слева выберите **"Authentication"**
2. Нажмите **"Начать"** или **"Get started"**
3. Перейдите на вкладку **"Способы входа"** (Sign-in method)
4. Включите **"Электронная почта/Пароль"** (Email/Password):
   - Нажмите на "Электронная почта/Пароль" (Email/Password)
   - Включите переключатель "Включить" (Enable)
   - Нажмите **"Сохранить"** (Save)

**Важно:** Используйте email/password для входа с любого устройства. Это позволит вам видеть одни и те же данные на всех устройствах.

## Шаг 4: Получение конфигурации

1. В меню слева выберите **⚙️ Настройки проекта** (Project settings)
2. Прокрутите вниз до раздела **"Ваши приложения"** (Your apps)
3. Нажмите на иконку **</>** (Web)
4. Зарегистрируйте приложение:
   - Введите название (например, `Subscription Tracker`)
   - **НЕ** устанавливайте Firebase Hosting (можно пропустить)
   - Нажмите **"Зарегистрировать приложение"**
5. Скопируйте объект конфигурации `firebaseConfig`

## Шаг 5: Добавление конфигурации в приложение

1. Откройте файл `index.html` в редакторе
2. Найдите блок с комментарием `// Конфигурация Firebase (замените на свою)`
3. Замените значения в объекте `firebaseConfig` на ваши:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSy...",                    // Ваш API ключ
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project.appspot.com",
    messagingSenderId: "123456789",
    appId: "1:123456789:web:abcdef"
};
```

4. Сохраните файл

## Шаг 6: Проверка работы

1. Откройте `index.html` в браузере
2. В правом верхнем углу вы увидите статус **"⚪ Локально"**
3. Нажмите кнопку **"Войти для синхронизации"**
4. В открывшемся окне:
   - Если у вас еще нет аккаунта: переключитесь на вкладку **"Регистрация"**, введите email и пароль (минимум 6 символов), нажмите **"Зарегистрироваться"**
   - Если аккаунт уже есть: введите email и пароль, нажмите **"Войти"**
5. Статус должен измениться на **"✅ Синхронизировано"** с вашим email
6. Добавьте тестовую подписку
7. Откройте приложение на другом устройстве/браузере
8. Войдите с тем же email и паролем - ваши данные должны синхронизироваться!

**Важно:** Используйте один и тот же email и пароль на всех устройствах, чтобы видеть одни и те же данные.

## 🔒 Безопасность

- Данные хранятся в Firestore и доступны только вам (через анонимную аутентификацию)
- Каждый пользователь имеет свой уникальный ID
- Правила безопасности Firestore защищают данные от несанкционированного доступа

## 💡 Примечания

- **Email/Password аутентификация**: Используйте один и тот же email и пароль на всех устройствах. Это позволит вам видеть одни и те же данные везде.
- **Офлайн режим**: Приложение продолжает работать локально, даже если нет интернета. Данные синхронизируются при восстановлении соединения.
- **Бесплатный тариф**: Firebase предоставляет бесплатный тариф (Spark Plan), которого достаточно для личного использования.
- **Безопасность**: Ваш пароль хранится в Firebase в зашифрованном виде. Никто, включая разработчиков, не может его увидеть.

## 🆘 Решение проблем

### Ошибка "Firebase не настроен"
- Убедитесь, что вы заменили конфигурацию в `index.html`
- Проверьте, что все поля заполнены правильно

### Данные не синхронизируются
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что вы вошли в систему (статус должен быть "✅ Синхронизировано")
- Проверьте правила Firestore - они должны разрешать чтение/запись для аутентифицированных пользователей

### Ошибка аутентификации
- Убедитесь, что анонимный вход включен в Firebase Console
- Проверьте, что проект Firebase активен

### Ошибка "Ошибка сохранения" при входе
Если при нажатии "Войти для синхронизации" появляется ошибка "Ошибка сохранения":

1. **Откройте консоль браузера (F12)** и проверьте детали ошибки:
   - Если ошибка `permission-denied` - проблема с правилами Firestore
   - Если ошибка `unavailable` - проблема с интернет-соединением
   
2. **Проверьте правила Firestore:**
   - Откройте Firebase Console → Firestore Database → Rules
   - Убедитесь, что правила выглядят так (БЕЗ символов ```):
     ```
     rules_version = '2';
     service cloud.firestore {
       match /databases/{database}/documents {
         match /users/{userId} {
           allow read, write: if request.auth != null && request.auth.uid == userId;
         }
       }
     }
     ```
   - Нажмите "Опубликовать" (Publish)
   
3. **Проверьте анонимную аутентификацию:**
   - Firebase Console → Authentication → Sign-in method
   - Убедитесь, что "Anonymous" включен
   
4. **Проверьте конфигурацию Firebase:**
   - Убедитесь, что в `index.html` правильно указаны все поля `firebaseConfig`
   - Проверьте, что нет опечаток в значениях
   
5. **Очистите кеш браузера:**
   - Нажмите Ctrl+Shift+Delete
   - Очистите кеш и cookies
   - Обновите страницу (Ctrl+F5)

## 📚 Дополнительные ресурсы

- [Документация Firebase](https://firebase.google.com/docs)
- [Firestore Documentation](https://firebase.google.com/docs/firestore)
- [Firebase Authentication](https://firebase.google.com/docs/auth)

---

**Готово!** Теперь ваши данные будут синхронизироваться между всеми устройствами. 🎉
